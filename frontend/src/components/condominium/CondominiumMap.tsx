export type StatusUnidade = "ocupada" | "vazia" | "alerta";
export interface UnidadeMapa { id: string; numero: string; status: StatusUnidade; }
const statusStyle: Record<StatusUnidade, string> = { ocupada: "fill-emerald-500/80 hover:fill-emerald-400", vazia: "fill-slate-700 hover:fill-slate-600", alerta: "fill-amber-500 hover:fill-amber-400" };
export default function CondominiumMap({ unidades, onSelect }: { unidades: UnidadeMapa[]; onSelect?: (unidade: UnidadeMapa) => void }) {
 return <svg viewBox="0 0 720 390" className="w-full min-w-[620px]" role="img" aria-label="Mapa interativo do condomínio"><path d="M20 335H700" stroke="currentColor" className="text-white/10" strokeWidth="26" /><text x="330" y="344" className="fill-white/35 text-[13px] font-bold">ACESSO / PORTARIA</text>{["A", "B", "C"].map((bloco, blocoIndex) => <g key={bloco} transform={`translate(${42 + blocoIndex * 228}, 42)`}><rect width="192" height="232" rx="16" className="fill-surface stroke-white/10" strokeWidth="2" /><text x="96" y="28" textAnchor="middle" className="fill-white text-[14px] font-black">BLOCO {bloco}</text>{unidades.slice(blocoIndex * 4, blocoIndex * 4 + 4).map((unidade, index) => <g key={unidade.id} transform={`translate(${20 + index % 2 * 82}, ${52 + Math.floor(index / 2) * 86})`} className="cursor-pointer" onClick={() => onSelect?.(unidade)}><rect width="70" height="62" rx="10" className={`${statusStyle[unidade.status]} transition-colors`} /><text x="35" y="37" textAnchor="middle" className="fill-white text-[13px] font-black">{unidade.numero}</text></g>)}</g>)}</svg>;
}
