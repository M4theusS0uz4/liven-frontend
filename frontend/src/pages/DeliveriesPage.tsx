import { PackagePlus, Search } from "lucide-react";
import { useMemo, useState } from "react";
type Entrega={id:number;codigo:string;morador:string;unidade:string;transportadora:string;status:"Recebida"|"Notificada"|"Retirada"};
const iniciais:Entrega[]=[{id:1,codigo:"LIV-4K7P",morador:"Ana Souza",unidade:"A-101",transportadora:"Correios",status:"Recebida"},{id:2,codigo:"LIV-8M2R",morador:"Carlos Lima",unidade:"B-202",transportadora:"Amazon",status:"Notificada"}];
export default function DeliveriesPage(){const [entregas,setEntregas]=useState(iniciais);const[busca,setBusca]=useState("");const filtradas=useMemo(()=>entregas.filter(e=>`${e.codigo} ${e.morador} ${e.unidade}`.toLowerCase().includes(busca.toLowerCase())),[busca,entregas]);const registrar=()=>setEntregas(lista=>[{id:Date.now(),codigo:`LIV-${Math.random().toString(36).slice(2,6).toUpperCase()}`,morador:"Morador a vincular",unidade:"—",transportadora:"Não informada",status:"Recebida"},...lista]);const retirar=(id:number)=>setEntregas(lista=>lista.map(e=>e.id===id?{...e,status:"Retirada"}:e));return <section className="space-y-8"><header className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-brand text-xs font-black tracking-[.25em]">PORTARIA</p><h1 className="text-3xl sm:text-4xl font-black mt-2">Gestão de entregas</h1><p className="text-white/45 mt-2">Registre, notifique e confirme retiradas.</p></div><button onClick={registrar} className="gradient-brand inline-flex items-center justify-center gap-2 rounded-xl px-5 py-3 text-sm font-black"><PackagePlus size={18}/>Nova entrega</button></header><label className="glass flex items-center gap-3 rounded-xl px-4 py-3"><Search size={18} className="text-white/45"/><input value={busca} onChange={e=>setBusca(e.target.value)} placeholder="Buscar por código, morador ou unidade" className="w-full bg-transparent outline-none placeholder:text-white/30"/></label><div className="glass overflow-hidden rounded-2xl"><div className="overflow-x-auto"><table className="w-full min-w-[640px] text-left text-sm"><thead className="border-b border-white/10 text-[11px] tracking-widest text-white/45"><tr><th className="p-5">CÓDIGO</th><th>MORADOR</th><th>TRANSPORTADORA</th><th>STATUS</th><th/></tr></thead><tbody>{filtradas.map(e=><tr key={e.id} className="border-b border-white/5 last:border-0"><td className="p-5 font-black">{e.codigo}</td><td><strong>{e.morador}</strong><span className="block text-white/45">{e.unidade}</span></td><td>{e.transportadora}</td><td><span className="rounded-full bg-brand/15 px-3 py-1 text-xs font-bold text-brand-light">{e.status}</span></td><td className="p-5">{e.status!=="Retirada"&&<button onClick={()=>retirar(e.id)} className="text-xs font-black text-emerald-400">Confirmar retirada</button>}</td></tr>)}</tbody></table></div></div></section>}
